{
	"name": "simply_deliver_tech_case",
	"version": "1.0.0",
	"main": "index.html",
	"license": "MIT",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"scss": "node-sass --watch scss -o css",
		"live": "live-server"
	},
	"devDependencies": {
		"live-server": "^1.2.2",
		"node-sass": "^8.0.0"
	}
}
